# The app pages below are marked noindex in their own HTML rather than blocked
# here. A Disallow would stop crawlers fetching the page at all, which means
# they never read the noindex and the bare URL can still surface in results.
# Crawl budget is not a concern at this size, so let them fetch and obey it.

User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://emptyinbox.me/sitemap.xml
