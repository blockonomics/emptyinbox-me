server {
  server_name _;
  listen 80;
  root /home/ubuntu/emptyinbox-me/static;
  index index.html;

  location /api {
    rewrite (^/api)(.*) $2 break;
    proxy_pass http://localhost:5000;
    proxy_pass_request_headers on;
  }
}